#!/usr/bin/env python

import Tools
import sys
import argparse
ap = argparse.ArgumentParser(description="Find reads with secondary alignments that do not overlap on the read with the primary alignment.")

